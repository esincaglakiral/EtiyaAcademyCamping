import { ProductFilteredPipe } from './product-filtered.pipe';

describe('ProductFilteredPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductFilteredPipe();
    expect(pipe).toBeTruthy();
  });
});
